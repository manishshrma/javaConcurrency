package org.threadpriority;

public class ThreadDemo {
    public static void main(String[] args) throws InterruptedException {
//        System.out.println(Thread.currentThread().getPriority()+" starting main Thread priority");
//        //now set the priority of main thread
//        Thread.currentThread().setPriority(6);
//
//        System.out.println(Thread.currentThread().getPriority()+" after changes now main thread priority");
//
//        ThreadPriority tp=new ThreadPriority(); // this thread will get its priority from the parent thread.
//        //here parenet thread is main thread which have changed priority is 7.
//        //so same is priority of child thread.
//        Thread t=new Thread(tp);
//        t.start();
      /////////////join////////////////////////

//        Thread jointhread=new Thread(new Join());
//        jointhread.start();
//        jointhread.join();//main thread won't run until the completion of jointhread.
//        System.out.println("now main thread run ---join concept");
//        ////imp ques- stop the child thread until the completion of main thread////
//        ////create a deadlock situation 2 ways/////////


        ////




    }
}
